This is a simple To-Do List web application built with JavaScript.

Users can add tasks, mark them as completed, and the tasks are saved using localStorage so they remain after refreshing the page.

This project demonstrates DOM manipulation, event listeners, and local storage.# todo-list
midterm
